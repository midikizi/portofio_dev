---
const skills = [
  {
    category: "Langages de programmation",
    items: ["Java", "JavaScript", "Python", "HTML5", "CSS3", "SQL", "PHP"]
  },
  {
    category: "Frameworks et bibliothèques",
    items: ["Spring Boot", "Django", "Angular", "Tailwind CSS", "Symfony", "Django REST"]
  },
  {
    category: "Bases de données",
    items: ["Oracle", "PostgreSQL", "MySQL", "MongoDB"]
  },
  {
    category: "Outils et Méthodologies",
    items: ["Git", "Docker", "Jaspersoft", "Jira", "Scrum", "UML", "Merise", "VS Code", "Eclipse", "IntelliJ IDEA"]
  }
];

---

<section id="skills" class="py-20 bg-gray-900/95 backdrop-blur-sm">
  <div class="container mx-auto px-4 lg:px-8 max-w-[1400px]">
    <div class="flex justify-between items-center mb-12">
      <h2 class="text-3xl font-bold text-purple-400 animate-fade-in">Compétences</h2>
      <a 
        href="/images/CV_2025-03-20_essodéké irené_MIDIKIZI.pdf" 
        target="_blank"
        class="inline-flex items-center px-6 py-3 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors shadow-lg hover:shadow-purple-500/20"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
        </svg>
        Consulter mon CV
      </a>
    </div>
    
      <!-- Badges -->
    <!--<div class="flex justify-center gap-8 mb-16 animate-fade-in">
      {badges.map(badge => (
        <div class="badge-container group">
          <div class="relative w-24 h-24">
            <img 
              src={badge.image} 
              alt={badge.alt}
              class="w-full h-full object-cover rounded-full shadow-lg transition-all duration-500 group-hover:scale-105" 
            />
            <div class="absolute inset-0 bg-gradient-to-br from-purple-500/10 to-transparent rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </div>
          <p class="text-center text-purple-300 mt-4 font-medium">{badge.name}</p>
        </div>
      ))}
    </div>-->

    <!-- Compétences techniques -->
    <div class="grid md:grid-cols-2 gap-8 mb-16">
      {skills.map(skill => (
        <div class="bg-gradient-to-br from-gray-800/50 to-purple-900/30 p-8 rounded-xl shadow-lg border border-purple-500/10 hover:border-purple-500/30 transition-all duration-300">
          <h3 class="font-semibold text-purple-300 text-xl mb-6">{skill.category}</h3>
          <div class="flex flex-wrap gap-3">
            {skill.items.map(item => (
              <span class="bg-purple-900/50 text-purple-300 px-4 py-2 rounded-full text-sm border border-purple-500/20 hover:border-purple-500/40 transition-colors">
                {item}
              </span>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- Certifications -->
    <!-- <div class="animate-fade-in">
      <h3 class="text-2xl font-bold text-purple-400 mb-8 text-center">Certifications</h3>
      <div class="grid md:grid-cols-2 gap-8">
        {certifications.map(cert => (
          <div class="bg-gradient-to-br from-gray-800/50 to-purple-900/30 rounded-xl shadow-lg border border-purple-500/10 overflow-hidden group hover:border-purple-500/30 transition-all duration-300">
            <div class="p-6">
              <div class="flex items-start gap-4">
                <div class="bg-emerald-500/20 p-3 rounded-lg">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />
                  </svg>
                </div>
                <div>
                  <h4 class="text-xl font-semibold text-purple-300 mb-2">{cert.title}</h4>
                  <p class="text-gray-400">{cert.platform}</p>
                  <p class="text-gray-400 text-sm">{cert.date}</p>
                  <p class="text-gray-400 text-sm">{cert.duration}</p>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div> -->
  </div>
</section>

<style>
  .animate-fade-in {
    animation: fadeIn 0.5s ease-in-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .badge-container {
    position: relative;
    display: inline-block;
  }

  .badge-container img {
    border: none;
    filter: drop-shadow(0 4px 6px rgba(139, 92, 246, 0.1));
  }

  .badge-container:hover img {
    filter: drop-shadow(0 8px 12px rgba(139, 92, 246, 0.2));
  }
</style>